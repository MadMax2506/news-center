import { FC } from 'react';

type WeatherCardSkeletonsProps = {};

export const WeatherCardSkeletons: FC<WeatherCardSkeletonsProps> = (props): JSX.Element => {
  const {} = props;
  return <>WeatherCardSkeletons</>;
};
